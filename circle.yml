general:
  branches:
    only:
      - master
      - production

machine: adjusting the VM to your preferences and requirements
  python:
    version: 2.7.6
  Node:
    version: 4.4.2
  services:
    - docker


checkout: 

dependencies: 

database: 

test: 

deployment: