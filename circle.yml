general:
  branches:
    only:
      - master
      - production

machine: 
  python:
    version: 2.7.6
  Node:
    version: 5.1.1
  services:
    - docker


checkout: 

dependencies: 

database: 

test: 

deployment:
