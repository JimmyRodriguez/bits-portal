general:
  branches:
    only:
      - master
      - production

machine: 
  python:
    version: 2.7.6
  node:
    version: 5.11.0
  services:
    - docker
  environment:
    DOCKER_TAG: $CIRCLE_BUILD_NUM-&CIRCLE_SHA1

dependencies: 
  pre:
    -apt-get update && upgrade
    -pip install awscli

database: 

test: 

deployment:
